<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Smart Inventário - Editar perfil</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha512-iBBXm8fW90+nuLcSKlbmrPcLa0OT92xO1BIsZ+ywDWZCvqsWgccV3gFoRBv0z+8dLJgyAHIhR35VZc2oM/gI1w==" crossorigin="anonymous" />
    {{ style('style') }}
  </head>
  <body>
    @!component('components/navbar', { name: userName })
    <section class="hero is-fullheight is-medium background-home is-bold">
      <div class="hero-body">
        <div class="container">
          <div class="columns is-centered">
            <div class='column is-5'>
              <div class="card is-rounded">
                <header class="card-header">
                  <p class="card-header-title">
                    Editar Perfil
                  </p>
                </header>
                <div class="card-content">
                  <form action="/saveedituser" method="POST">
                    {{ csrfField() }}
                    <div class="field">
                      <label class="label">Nome</label>
                      <div class="control">
                        <input name="name" class="input" type="text" placeholder="ex. Maria Joana" value="{{ userName }}">
                      </div>
                      @if(getErrorFor('name'))
                        <p class="help is-danger">O nome deve ter 3 caracteres ou mais</p>
                      @endif
                    </div>
                    <button type="submit" class="button is-primary">Enviar</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>
