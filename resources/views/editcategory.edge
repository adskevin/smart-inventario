<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Editar Categoria - Smart Inventário</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha512-iBBXm8fW90+nuLcSKlbmrPcLa0OT92xO1BIsZ+ywDWZCvqsWgccV3gFoRBv0z+8dLJgyAHIhR35VZc2oM/gI1w==" crossorigin="anonymous" />
    {{ style('style') }}
  </head>
  <body>
    @!component('components/navbar', { name: name })
    <section class="hero is-fullheight is-medium background-home is-bold">
      <div class="modal is-active" id="myModal">
        <div class="modal-background"></div>
        <div class="modal-card">
            <header class="modal-card-head">
              <p class="modal-card-title">Editar categoria</p>
            </header>
            <section class="modal-card-body">
              <form action="/editcategory" method="POST">
                {{ csrfField() }}
                <div class="field">
                  <label class="label">Descrição</label>
                  <div class="control">
                    <input name="description" class="input" type="text" value="{{ category.description }}">
                    <input hidden name="id" type="number" value="{{ category.id }}">
                  </div>
                  @if(getErrorFor('description'))
                    <p class="help is-danger">A descrição deve ter 3 caracteres ou mais</p>
                  @endif
                </div>
                <button type="submit" class="button is-primary">Salvar</button>
                <a href="/categories" class="button is-danger is-primary">Cancelar</a>
              </form>
            </section>
        </div>
      </div>
    </section>
  </body>
</html>
